---
import Layout from "../layouts/Layout.astro";
import More from "../components/More.tsx";
import ShareButton from "../components/ShareButton.tsx";
---

<Layout title="Count More">
  <main class="max-w-[768px] mx-6 md:mx-auto flex flex-row flex-wrap pt-12">
    <div class="w-full md:w-1/3 flex-shrink flex flex-col">
      <h1 class="text-7xl font-bold pb-4 pt-12">Count More</h1>
      <h3 class="text-2xl font-light pb-4">
        In the 2024 presidental election, every vote counts &mdash; but not all
        votes count the same. Find out where your vote counts more.
      </h3>
      <div>
        <ShareButton client:load />
      </div>
    </div>
    <div class="w-full md:w-2/3">
      <div class="border-4 mt-8 md:m-4 border-black p-12">
        <More client:load />
      </div>
    </div>
  </main>
</Layout>
